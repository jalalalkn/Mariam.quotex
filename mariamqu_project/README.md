# mariamqu

A pastel Android screen-analysis app for chart reading.

## Build

Use Codemagic or Gradle.

## Termux cleanup before uploading a new version

```bash
cd /sdcard/Download
rm -rf ~/mariamqu_project
rm -rf ~/old_mariamqu*
```

Then extract the new ZIP and push only the clean project folder.
